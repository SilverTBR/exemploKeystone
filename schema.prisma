// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource mysql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "mysql"
}

generator client {
  provider = "prisma-client-js"
}

model Fabricante {
  id       String    @id @default(cuid())
  nome     String    @default("")
  produtos Produto[] @relation("Produto_fabricantes")
}

model Produto {
  id             String        @id @default(cuid())
  nome           String        @default("")
  descricao      Json          @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  preco          Float
  fabricantes    Fabricante?   @relation("Produto_fabricantes", fields: [fabricantesId], references: [id])
  fabricantesId  String?       @map("fabricantes")
  Departamento   Departamento? @relation("Produto_Departamento", fields: [DepartamentoId], references: [id])
  DepartamentoId String?       @map("Departamento")

  @@index([fabricantesId])
  @@index([DepartamentoId])
}

model User {
  id        String    @id @default(cuid())
  name      String    @default("")
  email     String    @unique @default("")
  password  String
  createdAt DateTime? @default(now())
}

model Departamento {
  id       String    @id @default(cuid())
  nome     String    @default("")
  Produtos Produto[] @relation("Produto_Departamento")
}
